<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="memberSQL">
	<select id="getById" parameterType="String" resultType="spring.Member">
		SELECT * FROM A_MEMBER WHERE mem_id=#{id}
	</select>

	<insert id="memberInsert" parameterType="spring.Member">
		<!-- member는 spring.Member의 별칭. mybatis-config.xml 설정 -->
		insert into a_member 
		values(a_member_mem_num_seq.nextval,#{id},
		#{password}, #{name}, #{email}, #{photo}, #{introduce}, 1)
	</insert>
	
	<update id="memberUpdate" parameterType="spring.Member">
		update a_member set
		mem_nickname=#{name}, mem_password=#{password}
		, mem_email=#{email}, mem_photo=#{photo}, mem_introduce=#{introduce} where
		mem_id=#{id}
	</update>
	
	<delete id="memberDelete" parameterType="spring.Member">
		delete a_member where mem_id =
		#{id} and mem_password = #{password}
	</delete>
	<select id="loginCheck" parameterType="String" resultType="spring.Member">
		select
		* from a_member where mem_id = #{id}
	</select>
	
	<select id="getMemberList" resultType="spring.Member">
		select * from a_member
	</select>

</mapper>

